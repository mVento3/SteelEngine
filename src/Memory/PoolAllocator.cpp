#include "Memory/PoolAllocator.h"

namespace SteelEngine { namespace Memory {

    // template <typename A>
    // PoolAllocator<A>::PoolAllocator(void* startAddress, size_t maxSize) :
    //     Internal::PoolAllocator(startAddress, maxSize * sizeof(A), sizeof(A))
    // {

    // }

    // template <typename A>
    // PoolAllocator<A>::PoolAllocator(size_t maxSize) :
    //     Internal::PoolAllocator(maxSize * sizeof(A), sizeof(A))
    // {

    // }

    // template <typename A>
    // PoolAllocator<A>::PoolAllocator(size_t maxSize, Allocator* allocator) :
    //     Internal::PoolAllocator(maxSize * sizeof(A), sizeof(A), allocator)
    // {

    // }

}}